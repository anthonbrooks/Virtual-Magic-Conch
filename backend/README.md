# Magic Conch Backend

To start FastAPI server, enter:

```
uvicorn backend.main:app --reload
```

Server is now running at: http://127.0.0.1:8000

To update dependencies list:

```
pip freeze > requirements.txt
```
